Since we are using standard python libraries (like sqlite3, json, base64) for most features, you only need to install streamlit (for the web app) and fpdf (for generating the PDF tickets and receipts).